{
  "name": "Frontend Dev",
  "dockerComposeFile": "../docker-compose.dev.yml",
  "service": "frontenddev",
  "workspaceFolder": "/workspace",
  "shutdownAction": "none",
  "updateRemoteUserUID": false,
  "initializeCommand": "docker network inspect plantify-network >/dev/null 2>&1 || docker network create plantify-network",
  "postCreateCommand": [
  "chmod +x /workspace/.scripts/*.sh",
  "bash /workspace/.scripts/setup-dev.sh",
  "cd /workspace/plantify-react-app && npm install && npx playwright install --with-deps",
  "cd /workspace/Server && npm install"
],
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ]
}
