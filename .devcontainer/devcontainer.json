{
  "name": "Frontend Dev",
  "dockerComposeFile": "../docker-compose.dev.yml",
  "service": "frontenddev",
  "workspaceFolder": "/workspace",
  "shutdownAction": "none",
  "updateRemoteUserUID": false,
  "postCreateCommand": "bash -lc 'chmod +x /workspace/.scripts/*.sh && /workspace/.scripts/setup_dev.sh && cd /workspace/plantify-react-app && npm install && npx playwright install --with-deps && cd /workspace/Server && npm install'",
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ]
}
