name: buildApp CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install Frontend deps
      working-directory: ./plantify-react-app
      run: npm ci

    - name: Build Frontend
      working-directory: ./plantify-react-app
      run: npm run build --if-present
